The example configurator is in `TopoConfigurators/Standard`

Before start the configurator, please edit the Makefile set the proper ADDR and PORT env.

And use `make run` command in the workdir to start it.